# OTC Prince Transcript Form v2

This revision keeps semantic HTML form controls but compresses the visual layout to more closely match the existing one-page OTC South Campus transcript request form.

## Build

```bash
./build.sh
```

## Validate

```bash
./validate.sh
```

The build uses Prince with interactive PDF forms and PDF/UA-1 output.
